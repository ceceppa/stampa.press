.stampa-field {
  &--heading {
    h2 {
      &::before {
        display: none !important;
      }
    }

    textarea {
      font-size: inherit;
      color: inherit;
    }

    .stampa-block & {
      font-size: 1rem;
      padding: 0;
      margin: 0;
    }
  }
}
