const { __ } = wp.i18n;
const { MediaUpload } = wp.editor;
const { IconButton } = wp.components;

export default function StampaPostSelector({
  fieldName,
  image,
  attributes,
  updateAttribute,
  updateFocusedField,
}) {}
